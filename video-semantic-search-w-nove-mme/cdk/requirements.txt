aws-cdk-lib>=2.170.0
constructs>=10.0.0
# Capped below 3.0: cdk-nag 3.x removed the top-level NagSuppressions,
# NagPackSuppression and AwsSolutionsChecks exports that app.py and
# nag_suppressions.py import, so an unpinned install fails at synth with
# "cannot import name 'NagSuppressions' from 'cdk_nag'".
cdk-nag>=2.28.0,<3
